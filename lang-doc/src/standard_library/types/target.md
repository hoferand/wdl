# Target

```wdl
{
    stations?: [string],
    stationareas?: [string],
    waypoints?: [{ x: number, y: number }],
    not?: {
        stations?: [string],
        stationareas?: [string],
        waypoints?: [{ x: number, y: number }],
    }
}
```
